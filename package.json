{
  "name": "models-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "catalog:audit": "node scripts/audit.ts",
    "catalog:audit:refresh": "node scripts/audit.ts --write && oxfmt catalog/schema-inventory.json",
    "catalog:check": "pnpm build && node scripts/catalog.ts && node scripts/overlays.ts && node scripts/audit.ts",
    "catalog:refresh": "pnpm build && node scripts/catalog.ts --write && node scripts/overlays.ts --write && node scripts/audit.ts --write && oxfmt catalog/schema-inventory.json",
    "check": "pnpm format:check && pnpm icons:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm check:packages",
    "check:packages": "publint packages/core --strict && publint packages/providers --strict && publint packages/elements --strict && publint packages/ai-sdk --strict && node scripts/checkPackages.ts",
    "dev": "pnpm --filter @models/gallery dev",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "icons:check": "node scripts/icons.ts",
    "icons:refresh": "node scripts/icons.ts --write",
    "lint": "oxlint --type-aware --deny-warnings .",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@ai-sdk/anthropic": "^4.0.46",
    "@ai-sdk/google": "^4.0.58",
    "@ai-sdk/openai": "^4.0.52",
    "@arethetypeswrong/cli": "^0.18.5",
    "@changesets/cli": "^3.0.1",
    "@lobehub/icons-static-svg": "1.94.0",
    "@types/node": "^26.4.0",
    "ai": "^7.0.85",
    "happy-dom": "^20.12.0",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "oxlint-tsgolint": "^7.0.2001",
    "publint": "^0.3.24",
    "tsdown": "^0.22.14",
    "typescript": "^6.0.3",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.17.1"
}
