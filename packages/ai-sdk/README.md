# @models/ai-sdk

A small optional bridge from a validated Models selection to call options that
can be spread into AI SDK `generateText` or `streamText`. See the
[repository](https://github.com/axel-rock/models) for examples.
